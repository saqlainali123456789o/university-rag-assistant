streamlit>=1.38,<2.0
langchain-core>=0.3
langchain-community>=0.3
langchain-huggingface>=0.1
faiss-cpu>=1.9
sentence-transformers>=3.0
groq>=0.11

# NOTE: after your first successful local run, freeze exact versions with:
#   pip freeze > requirements.txt
# This locks the deploy so a future upstream release can't silently break it.
